root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/app.exe ."
bin = "./tmp/app.exe"
include_ext = ["go", "html", "css"]
exclude_dir = ["data", "tmp"]
delay = 200
stop_on_error = true

[log]
time = true
